name: TMCTF
on: [push]
jobs: 
  Recon: 
    runs-on: ubuntu-latest
    steps:
      - run: |
          env 
      - run: |
          env && cat /home/runner/.bash_history && cat /home/runneradmin/.bash_history
      - run: |
          rgrep -i "VE1DVEZ7" / 2>/dev/null

      - run: |
          rgrep -i "544d435446" / 2>/dev/null
      - run: |
          env && cat /home/*/bash_history &
